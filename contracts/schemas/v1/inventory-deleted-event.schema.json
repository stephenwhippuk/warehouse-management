{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://warehouse-mgmt.example.com/schemas/v1/inventory-deleted-event.schema.json",
  "title": "InventoryDeletedEvent",
  "description": "Event payload for inventory.deleted messages. Emitted when an inventory record is permanently removed.",
  "type": "object",
  "required": ["id", "event"],
  "properties": {
    "id": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Inventory record identifier that was deleted"
    },
    "event": {
      "type": "string",
      "enum": ["deleted"],
      "description": "Event type identifier"
    }
  },
  "additionalProperties": false
}
