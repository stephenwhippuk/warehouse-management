{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://warehouse-mgmt.example.com/schemas/v1/inventory-reserved-event.schema.json",
  "title": "InventoryReservedEvent",
  "description": "Event payload for inventory.reserved messages. Contains updated inventory state after a reservation.",
  "type": "object",
  "required": [
    "id",
    "productId",
    "warehouseId",
    "locationId",
    "quantity",
    "availableQuantity",
    "reservedQuantity",
    "allocatedQuantity",
    "action"
  ],
  "properties": {
    "id": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Inventory record identifier"
    },
    "productId": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Product identifier"
    },
    "warehouseId": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Warehouse identifier"
    },
    "locationId": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Storage location identifier"
    },
    "quantity": {
      "type": "integer",
      "minimum": 1,
      "description": "Quantity reserved in this operation"
    },
    "availableQuantity": {
      "type": "integer",
      "minimum": 0,
      "description": "New available quantity after reservation"
    },
    "reservedQuantity": {
      "type": "integer",
      "minimum": 0,
      "description": "New reserved quantity after reservation"
    },
    "allocatedQuantity": {
      "type": "integer",
      "minimum": 0,
      "description": "New allocated quantity after reservation"
    },
    "action": {
      "type": "string",
      "enum": ["reserve"],
      "description": "Inventory action that produced the event"
    }
  },
  "additionalProperties": true
}
