{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://warehouse-mgmt.example.com/schemas/v1/inventory-deallocated-event.schema.json",
  "title": "InventoryDeallocatedEvent",
  "description": "Event payload for inventory.deallocated messages. Contains updated inventory state after a deallocation.",
  "type": "object",
  "required": [
    "id",
    "productId",
    "warehouseId",
    "locationId",
    "quantity",
    "availableQuantity",
    "reservedQuantity",
    "allocatedQuantity",
    "action"
  ],
  "properties": {
    "id": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Inventory record identifier"
    },
    "productId": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Product identifier"
    },
    "warehouseId": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Warehouse identifier"
    },
    "locationId": {
      "$ref": "common.schema.json#/$defs/uuid",
      "description": "Storage location identifier"
    },
    "quantity": {
      "type": "integer",
      "minimum": 1,
      "description": "Quantity deallocated in this operation"
    },
    "availableQuantity": {
      "type": "integer",
      "minimum": 0,
      "description": "New available quantity after deallocation"
    },
    "reservedQuantity": {
      "type": "integer",
      "minimum": 0,
      "description": "New reserved quantity after deallocation"
    },
    "allocatedQuantity": {
      "type": "integer",
      "minimum": 0,
      "description": "New allocated quantity after deallocation"
    },
    "action": {
      "type": "string",
      "enum": ["deallocate"],
      "description": "Inventory action that produced the event"
    }
  },
  "additionalProperties": true
}
